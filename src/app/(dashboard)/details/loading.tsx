import {Container, Loader} from '@mantine/core'
import React from 'react'

export default function Loading() {
    return (
        <Container>
            <Loader size={20} />
        </Container>
    )
}
